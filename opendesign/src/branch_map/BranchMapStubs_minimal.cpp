#include "branch_map/BranchMapStubs.h"

namespace branch_map {

// Minimal implementation to satisfy the linker
void ensure_branch_map_registrations() {
    // Empty implementation - just satisfies the linker for now
    // This replaces the complex stub functions that were causing build issues
}

}  // namespace branch_map